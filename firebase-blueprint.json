{
  "entities": {
    "users": {
      "title": "User",
      "description": "A user of the portfolio website.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The unique ID of the user." },
        "name": { "type": "string", "description": "The full name of the user." },
        "email": { "type": "string", "description": "The email address of the user." },
        "photoURL": { "type": "string", "description": "The URL of the user's profile photo." },
        "role": { "type": "string", "enum": ["admin", "user"], "description": "The role of the user." },
        "createdAt": { "type": "string", "format": "date-time", "description": "The time the user was created." },
        "lastLogin": { "type": "string", "format": "date-time", "description": "The time the user last logged in." },
        "lastActive": { "type": "string", "format": "date-time", "description": "The time the user was last active." }
      },
      "required": ["uid", "name", "email", "role"]
    },
    "activity_logs": {
      "title": "Activity Log",
      "description": "A log of user activity on the website.",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "The ID of the user who performed the action." },
        "name": { "type": "string", "description": "The name of the user." },
        "email": { "type": "string", "description": "The email of the user." },
        "action": { "type": "string", "description": "The action performed (e.g., LOGIN, VISITED_HOME)." },
        "timestamp": { "type": "string", "format": "date-time", "description": "The time the action was performed." },
        "page": { "type": "string", "description": "The page where the action was performed." },
        "metadata": { "type": "object", "description": "Additional metadata about the action." }
      },
      "required": ["userId", "action", "timestamp"]
    },
    "contact_submissions": {
      "title": "Contact Submission",
      "description": "A message submitted via the contact form.",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "The name of the person who submitted the form." },
        "email": { "type": "string", "description": "The email of the person who submitted the form." },
        "message": { "type": "string", "description": "The message content." },
        "timestamp": { "type": "string", "format": "date-time", "description": "The time the form was submitted." },
        "status": { "type": "string", "enum": ["new", "read", "replied"], "description": "The status of the submission." }
      },
      "required": ["name", "email", "message", "timestamp", "status"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "users",
      "description": "User profiles."
    },
    "/activity_logs/{logId}": {
      "schema": "activity_logs",
      "description": "Logs of user activity."
    },
    "/contact_submissions/{submissionId}": {
      "schema": "contact_submissions",
      "description": "Contact form submissions."
    }
  }
}
